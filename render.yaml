services:
  - type: web
    name: logiport-mart
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: SQLITE_DB_PATH
        value: /tmp/database.sqlite
